"use client";

/**
 * Re-export the official Auth Agent button component from the SDK
 * This uses Auth Agent's official React component instead of a custom wrapper
 */
export { AuthAgentButton } from "auth-agent-sdk/client/react";
